using System.Globalization;
using anonymous_message.Application.Common.Interfaces;
using anonymous_message.Application.TodoLists.Queries.ExportTodos;
using anonymous_message.Infrastructure.Files.Maps;
using CsvHelper;

namespace anonymous_message.Infrastructure.Files;

public class CsvFileBuilder : ICsvFileBuilder
{
    public byte[] BuildTodoItemsFile(IEnumerable<TodoItemRecord> records)
    {
        using var memoryStream = new MemoryStream();
        using (var streamWriter = new StreamWriter(memoryStream))
        {
            using var csvWriter = new CsvWriter(streamWriter, CultureInfo.InvariantCulture);

            csvWriter.Configuration.RegisterClassMap<TodoItemRecordMap>();
            csvWriter.WriteRecords(records);
        }

        return memoryStream.ToArray();
    }
}
